<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
  <data>
    <variable
        name="pokemonItem"
        type="com.example.domain.model.DetailPokemonModel"
        />
  </data>
  <androidx.appcompat.widget.Toolbar
      android:layout_width="match_parent"
      android:layout_height="?attr/actionBarSize"
      android:minHeight="56dp"
      android:orientation="horizontal"
      android:gravity="center_horizontal"
      >
    <ImageView
        android:id="@+id/ic_back"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentStart="true"
        android:layout_margin="@dimen/dimens_16"
        android:layout_gravity="center_vertical"
        android:src="@drawable/ic_back_arrow"
         />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tv_toolbar_pokemon_name"
        android:textSize="18sp"
        android:text="@{pokemonItem.name}"
        android:fontFamily="@font/poppin_semi_bold"
        android:textColor="@color/white"
        />
    <TextView
        android:id="@+id/tv_toolbar_pokemon_id"
        android:layout_width="wrap_content"
        android:textSize="18sp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dimens_16"
        android:fontFamily="@font/poppin_semi_bold"
        android:textColor="@color/white"
        android:text="@{String.valueOf(pokemonItem.id)}"
        android:layout_gravity="end"
        />
  </androidx.appcompat.widget.Toolbar>


</layout>
